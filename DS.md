# DS ROADMAP:

* BigO notation:
  * time complixity/space complexity
  --> linear time
  -->logarithmic time

* Arrays-> time complexity
  * big arrays vs dynamic arrays(arrays that can grow in size)

* linked list
* Creating/ Inserting/Delrting/Locating:
which data structure to pick for specific operations

* double/ single linked list

* Queu vs stack(lifo/fifo...)

* Trees (not linear data)

  * binary tree

  * binary search tree

  * heaps

  * Graphs((un)weighted/(un)Directed Graphs)

* Hashing

* ALgorithms

  * Recursion

  * searching ( linear/ binary..)

  * sorting (insertion/selection/bubble..)

  * Kruskal algorithms

  * path finding algorithm

  * greedy / dividing conquer/ dynamic programming/backtraking (learn by exercising)

  -----
  ## Stack

  - last in last out
  - use push() to add, pull()to remove 
  if i need to access the first object i need to pop everything above it
  - it is possible to run out of memory when using stack
 ### stack uses
  - used in undo/ redo features 
  - used in backtracking in algorithms
  - used in calling functions
  ------
  ## queue
  FiFo: first in first out (first come first served)
- enqueu (add object to the end(tail)) **offer()**
-dequeu(remove objects from the begining) **poll()**


